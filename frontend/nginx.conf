server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # For Single Page Applications like React, fallback all non-file routes to index.html
        try_files $uri $uri/ /index.html;
    }
}
